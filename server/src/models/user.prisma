model User {
  id             Int             @id @default(autoincrement())
  email          String          @unique
  username       String
  profilePicture String
  UserWorkspace  UserWorkspace[]
}
